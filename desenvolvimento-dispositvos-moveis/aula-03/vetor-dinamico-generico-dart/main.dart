//VetorDinamico<T> - O "<T>" signifca classe genérica, ela pode 
// armazenar qualquer tipo de dado (inteiro, string, objetos, etc.)

// Atividade

// 1. O vetor deve ser genérico: deve funcionar com int, double, String, ou qualquer outro tipo.

// 2. Deve possuir uma capacidade inicial (por padrão, 4 elementos).

// 3. Sempre que faltar espaço para uma nova inserção, a capacidade deve dobrar (ex.: 4 → 8 → 16).

// 4. A classe deve armazenar os elementos em uma lista interna (List<T?>) e controlar a quantidade real de elementos inseridos com um contador (_tamanho).

// 5. O acesso aos elementos deve respeitar os limites do vetor. Qualquer tentativa de acessar uma posição inválida deve lançar um “RangeError”.

// 6. As operações de adição, remoção e inserção devem manter o vetor consistente, deslocando elementos quando necessário.

// 7. O método toString() deve imprimir apenas os elementos válidos.

